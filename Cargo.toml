[package]
name = "update-toolkit"
version = "0.1.0"
edition = "2021"
description = "Silent updater helper for replacing app.asar without console window"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
strip = "symbols"
